{
  "name": "sharibo",
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "circuits",
    "packages/client",
    "scripts",
    "app"
  ],
  "scripts": {
    "e2e": "npm run e2e --workspace=scripts",
    "test": "npm test --workspaces --if-present",
    "lint:dead": "knip",
    "check:stellar-sdk": "node scripts/check-stellar-sdk-version.mjs"
  },
  "devDependencies": {
    "knip": "6.33.0"
  }
}
