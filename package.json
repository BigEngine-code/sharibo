{
  "name": "sharibo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "circuits",
    "packages/client",
    "scripts",
    "app"
  ],
  "scripts": {
    "e2e": "npm run e2e --workspace=scripts",
    "check:stellar-sdk": "node scripts/check-stellar-sdk-version.mjs",
    "postinstall": "npm run check:stellar-sdk"
  },
  "overrides": {
    "@stellar/stellar-sdk": "^16.0.1"
  }
}
